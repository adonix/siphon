class SiteIndexController < ApplicationController
  def home
    #@user = User.find(1)
    #@video = @user.videos.build
  end

  def login
  end

  def upload
    #@video = @user.videos.find(p)
  end

  def search
  end
end
